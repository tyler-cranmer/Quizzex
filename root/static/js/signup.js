/*
    signUp will get the form with the username, email, password, & confirm password variables
    It will then send this form to the Flask file
*/
function signUp() {
    $("#SignupForm").submit();
}